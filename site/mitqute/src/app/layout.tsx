import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "MITqute - Quantum Matchmaking Revolution",
  description: "Discover your quantum match with MITqute. Dive into the new era of matchmaking powered by quantum computing and find connections that transcend the conventional, all within the MIT community.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
